name: Reuseable workflow
on: workflow_call  # this is used for reuseable purpose.
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: echo "Deploying and uploading..."

